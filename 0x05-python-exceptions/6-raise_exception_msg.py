#!/usr/bin/python3


def raise_exception_msg(message=""):
    """ raises name exception with a message

    args:
        message: doesn't receive any argument
    Returns: doesn't return anything
    """
    raise NameError(message)
